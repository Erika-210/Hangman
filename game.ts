import { prompt } from "./inputHelper"

const main = async () => {
    // Set the word (import the getRandomWord function)


    // Initialize game state (misses, guessed letters, etc.)


    // Game loop (prompt for letter, validate input, check word for letter, etc.)
    while (true /* replace with checks for game over / win condition */) {

    }
}

main()